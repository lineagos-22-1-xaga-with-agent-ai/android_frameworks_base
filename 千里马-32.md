# ZygoteInit.java 中 SystemServer 的启动流程分析

目标文件：`core/java/com/android/internal/os/ZygoteInit.java`

## 1. 入口：ZygoteInit.main() 是否拉起 system_server
在 `ZygoteInit.main(String[] argv)` 里，会解析启动参数：
- 当参数里包含 `start-system-server` 时，`startSystemServer = true`。
- 随后执行：`forkSystemServer(abiList, zygoteSocketName, zygoteServer)`。

也就是说，是否启动 `system_server` 是由 zygote 启动参数决定的，不是每次 fork 应用都会走这条路径。

## 2. forkSystemServer()：构造 system_server 进程启动参数
`forkSystemServer(...)` 做了几件关键事：

1. 计算并裁剪 Linux capabilities。
2. 组装硬编码参数 `args`，核心包括：
- `--setuid=1000` / `--setgid=1000`（即 `system` 用户）
- 一组 `--setgroups=...`
- `--capabilities=...`
- `--nice-name=system_server`
- `--target-sdk-version=...`
- 启动类：`com.android.server.SystemServer`

3. 解析为 `ZygoteArguments parsedArgs`，并附加运行时策略：
- 内存标记（MTE/TBI）相关 runtime flags
- `GWP_ASAN_LEVEL_LOTTERY`
- 可选 `PROFILE_SYSTEM_SERVER`

4. 调用 `Zygote.forkSystemServer(...)` 真正 fork。

## 3. fork 后分叉：父进程与子进程
`pid = Zygote.forkSystemServer(...)` 后：

- `pid > 0`（父进程，仍是 zygote）：返回 `null`，继续进入 socket select loop，等待后续孵化请求。
- `pid == 0`（子进程，即未来 system_server）：
  - 若存在双 zygote（32/64），先 `waitForSecondaryZygote(...)`。
  - 关闭继承来的 zygote server socket：`zygoteServer.closeServerSocket()`。
  - 进入 `handleSystemServerProcess(parsedArgs)`。

## 4. handleSystemServerProcess()：把“子进程”变成 system_server
该方法完成 system_server 的 Java 层收尾初始化：

1. `umask(0077)`，设置默认文件权限。
2. 设置进程名：`Process.setArgV0("system_server")`（由 `mNiceName` 提供）。
3. 读取 `SYSTEMSERVERCLASSPATH`：
- 可选做 profile 准备（debug/eng + 开关打开时）。
4. 两条执行路径：
- 若 `mInvokeWith != null`：走 `WrapperInit.execApplication(...)`（wrapper 启动）。
- 常规路径：
  - 创建/获取 system_server 专用 ClassLoader：`getOrCreateSystemServerClassLoader()`。
  - `Thread.currentThread().setContextClassLoader(cl)`。
  - 调用 `ZygoteInit.zygoteInit(...)`。

## 5. zygoteInit() 到 SystemServer.main()
`ZygoteInit.zygoteInit(...)` 的关键顺序：

1. `RuntimeInit.commonInit()`
2. `ZygoteInit.nativeZygoteInit()`（native 层初始化，含 Binder 线程池相关入口）
3. `RuntimeInit.applicationInit(...)`

`RuntimeInit.applicationInit(...)` 会解析参数并调用：
- `RuntimeInit.findStaticMain(args.startClass, args.startArgs, classLoader)`

而 `startClass` 正是前面传入的 `com.android.server.SystemServer`，所以最终会反射拿到并返回 `SystemServer.main(String[] args)` 的调用入口（`MethodAndArgsCaller`），随后在 `ZygoteInit.main()` 中 `r.run()` 执行。

## 6. 一句话总结
`system_server` 的本质是：
- 由 zygote 在启动阶段根据 `start-system-server` 参数 fork 出子进程，
- 子进程经 `handleSystemServerProcess()` 做权限/类加载器/运行时初始化，
- 最终通过 `RuntimeInit.findStaticMain()` 跳转执行 `com.android.server.SystemServer.main()`。
